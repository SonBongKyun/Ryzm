# ═══════════════════════════════════════════════
#  Ryzm Terminal — Environment Variables
#  Copy to .env and fill in actual values.
# ═══════════════════════════════════════════════

# ── App Basic ──
APP_ENV=production
APP_BASE_URL=https://your-domain.com
APP_VERSION=1.0.0
HOST=0.0.0.0
PORT=8000

# ── Security ──
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING_64_CHARS
JWT_EXPIRE_HOURS=168

# ── Admin ──
ADMIN_TOKEN=change-me-strong-token
ADMIN_EMAILS=admin@example.com

# ── AI (Required) ──
GENAI_API_KEY=your-google-genai-api-key

# ── Database ──
# For SQLite (default): leave empty
# For Postgres (production): postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_URL=

# ── Stripe (Subscription Payments) ──
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID_PRO=price_...
SITE_URL=https://your-domain.com

# ── Email / SMTP ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@your-domain.com
BASE_URL=https://your-domain.com

# ── Discord (Daily Briefing Webhook) ──
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_URL

# ── Feature Kill Switches (1-person ops safety) ──
# Set to false to temporarily disable AI features (cost/incident)
ENABLE_COUNCIL=true
ENABLE_CHAT=true
ENABLE_VALIDATOR=true

# ── Beta Access Gate ──
# Set a code to require invite code on registration. Empty = open registration.
BETA_INVITE_CODE=

# ── Rate Limits (Pro users, per minute) ──
RATE_LIMIT_COUNCIL_PER_MIN=30
RATE_LIMIT_CHAT_PER_MIN=60
RATE_LIMIT_VALIDATOR_PER_MIN=20

# ── Cache TTL ──
CACHE_TTL=300
MARKET_CACHE_TTL=60

# ── CORS ──
ALLOWED_ORIGINS=https://your-domain.com,http://localhost:8000

# ── Error Tracking (Optional) ──
# Install sentry-sdk to enable: pip install sentry-sdk
SENTRY_DSN=

# ── CoinGecko (Optional — improves rate limits) ──
CG_DEMO_API_KEY=

# ── Trial Duration ──
PRO_TRIAL_DAYS=7

# ── PostgreSQL Pool Size (Optional) ──
PG_POOL_MIN=2
PG_POOL_MAX=10

# ── Yahoo Finance (Optional) ──
ENABLE_YAHOO=true
